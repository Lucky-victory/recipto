import { NavTitle, Navbar, Page } from 'framework7-react';
import React from 'react';

const ProfilePage = () => {
    return (
        <Page name="profile">
            <Navbar>
                <NavTitle></NavTitle>
            </Navbar>
        </Page>
    );
};

export default ProfilePage;
